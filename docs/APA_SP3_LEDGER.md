# APA-SP3 ledger

2026-09-06 — Read HOUSE_RULES and immutable order. No git, subagents, GPU execution or foreign process signals. Runtime metadata: gpt-6-astra, xhigh (logs/apa_sp3_r1.log). Memory used only for evidence-class discipline; live sources govern.

Pre-gate registration: d9b6511702a894f72174795141c72b2097b1bbe6110e810a1d4d8bfc3cd3498c. Token SHA is null, explicitly RED: named graft harness has no PPL protocol; temporary original scripts absent. Recovery harness uses a different corpus/window. Requested original provenance from lead while continuing CPU plumbing. No substitution registered. SP2/SPD1 docs absent in this worktree; read named documents in their sibling worktrees without execution or edits. SP1 parent hashes sourced through SP2 registration and corroborated in local SP1 ledger.

Code inspection: D=96 and VD=96 select cap128 in both SP paths; no launcher edit needed. BF16 reconstructed TurboQuant keys are not packed FP4 execution. Current standard and blend arithmetic differs from historical 20.065-era code; parity is not presumed.

## Prior art

- BLASST: Yuan et al. 2025/2026, arXiv:2512.12087: running-max log threshold; APA promotes individual keys instead of omitting blocks
- ThriftAttention: Sharratt 2026, arXiv:2605.23081: selective precision and weight-sensitive error motivation; no FP4 tensor-core implementation port
- FlashAttention-2: Dao 2023, arXiv:2307.08691 and Milakov/Gimelshein 2018 online normalizer: stable online softmax/work partitioning; existing SP implementation reused, no FA2 port
- TurboQuant: Zandieh et al. 2025, arXiv:2504.19874: rotation and scalar codebook via existing quant.py; reconstructed BF16 keys, no QJL residual or packed FP4 arithmetic
- APA: David Perry 2026, docs/APA_PAPER_DRAFT.md: all-key denominator, |bulk| z selection and precision promotion
- SP2: existing conditional log(1/epsilon)+2eq proof; here model finite max measurement, no new universal theorem
- SP3: new experiment wiring, global model calibration, captured-activation tail audit and fail-closed receipts; empirical percentiles and matched-control experiments are standard, no novelty claimed

Literature metadata verified from arXiv abstracts for 2512.12087, 2605.23081, 2504.19874, 2307.08691; BLASST running-max criterion verified from v3 HTML section 3.1. Local APA draft and lead comparison read. No pre-existing kernel body edited.

Host build PASS: timeout 575s bash scripts/apa_sp3_build.sh, log artifacts/apa_sp3/build.log; core engine and separate literal diagnostic copies compiled/linked for sm_89. Every production source file remains hash-identical. Weight hashing verified f4c69dfe493d3b644071e1b3820d68f49bcd6674cd40e71b9e451b8534586613 in 22.08s, no model/GPU load. Diagnostic module imports on CPU.

First CPU baseline: 17 PASS / 1 FAIL. Exact failure: ValueError operands could not be broadcast together with shapes (4,96) (4,9). Root cause: new test passed shared V to existing online_batch which requires batched V. Fixed only the new test call by broadcasting V to (4,9,96), no algorithm/tolerance change. Initial failure retained in cpu_baseline.log.

Plumbing additions: native B diagnostic copies preserve output and append mask stores; observed B calls require byte-identical native/copy outputs on the actual inputs. Existing SP diagnostics reused. Full q/k/kq + bitpacked native masks captured separately from timings. G2 uses every eligible pair and exact nearest-rank float32-stored error quantiles, float64 sum/max; per-layer jobs. This can require >40 GB per 8192 capture; disk failure is RED, no sampling fallback. Runtime resident peak is explicitly an estimate: intercepted cudaMalloc high-water plus pre-call device/context offset, may miss driver-internal transients. No background monitor thread. Prior art: standard ELF interposition and allocation accounting, not a new optimization.

Final gates: 20 CPU tests passed (0 failed, 0 skipped), cpu_delivery.log. Final mutation run froze mutation_manifest_final.json before baseline/defects; all six nonerror semantic mutants killed (100%, threshold 80%). Original unmanifested seed-run receipt retained separately; final registered run supersedes it for author-baseline coverage. No production mutations. Blind verification remains lead-owned.

Source-audit correction BEFORE any GPU result: amendment_002_native_bulk.json replaces ideal bulk dots with a native SP warp/FMA-order score probe at actual fp32 scale; amendment_003_B_native_scores.json adds hash-verified replay of actual B cuBLAS bulk chunks. G2 now reports actual-arm error and separate sp_error used for E max; no percentile substituted for a bound. C global calibration still validates actual native fraction and never sees PPL. E has its own separate finite-envelope transfer capture/check jobs. Native probe/copy parity is a GPU pin, not CPU-proven.

Execution amendment_001 budgets 480s worker + 5s own-child grace, 20s lease wait, 15s preflight, 10s device probe, 10s receipt publication and 30s foreground cooldown (570s plus slack); outer TERM at 585s and 3s grace. This tightens original 510/30 limits before GPU work. No mathematical tolerance, prediction or source kernel changed.

Read-only adapter plumbing imported successfully on CPU: core, adapters, adapters.tokenizer, core.mistral7b_tc, core.qwen_tc, core.minicpm3_tc, each SHA-pinned in adapter_import_cpu.json. Engine module location pinned to this worktree build. Weight file full SHA reverified with stable inode/size/mtime/ctime identity; no weights loaded to GPU. cudaGetDeviceCount returned 100, verbatim: no CUDA-capable device is detected.

Actual lead-runner preflight check: timeout 20s bash scripts/apa_sp3_lead_gpu.sh run g0 exited 1, before lease or GPU work. Verbatim: Red: G0 BLOCKED_PROTOCOL: original guide tokens/scoring source missing; token SHA null. g0_preflight_block.log retained. No model jobs/quality numbers produced.

Delivery matrix: 688 cells including primary B/C all-pair margins (248 layer jobs), optional bulk8, E transfer checks, and 32-step decode at 2048/8192/32768. Every job is foreground bounded. Partial/OOM/timeout/stale/RED receipts cannot masquerade as completed rows; G0 or D hard rail writes a global model-stop receipt. The reporter emits results.json, margins.json and RESULTS.md with evidence classes, two-paragraph G2 reading, per-job estimates and exact commands.

RED residuals: immutable registration token SHA remains null and exact scoring implementation is conditional on recovered source matching last-512-within-input convention. Original guide prefix/scorer is still missing; no alternate corpus used. Existing standard arithmetic is known to have changed since historical receipts; no numerical rollback attempted. Native CUDA tests, all model gates, peak-resident estimate validation and lead blind verification unrun. Full standard 32768 prefill alone allocates an 80 GiB BF16 score tensor; the registered full-prefill decode cache setup cannot fit 12 GB. This does NOT establish that cached standard single-token decode itself cannot fit; a different cache-preparation protocol would require a separate pre-card amendment, not an OOM fallback here. Long B/SP prefill may hit the 480s ceiling.

Prior art at final sites: A ordinary SDPA (Vaswani et al. 2017, arXiv 1706.03762); B Perry APA draft plus existing TurboQuant reconstruction; C/D BLASST-style comparator and existing online-softmax/split merge; E SP2 conditional two-sided error margin. G2 uses ThriftAttention weight-sensitive motivation and standard empirical/order-statistic measurements. G3 is a standard controlled cached teacher-forced timing experiment. Probe/copy instrumentation, model calibration and receipt wiring are this seat's implementation, with no new selection/proof/quantizer claim. Online normalizer reference Milakov/Gimelshein 2018, arXiv 1805.02867, verified. Mutation historical lead DeMillo/Lipton/Sayward 1978 was checked in bibliographic search; paper not fully read.

Process audit: no git, no subagents, no shell background jobs, no model/GPU workloads, no foreign PID signals, no service changes, no writes to read-only adapters/cache/production source. Own bounded read-only rg discovery once timed out after traversing Shared links; no success inferred from incomplete scan. All started foreground builds, reads and tests completed. Author model gpt-6-astra, reasoning effort xhigh (launch log). Registration hash still d9b6511702a894f72174795141c72b2097b1bbe6110e810a1d4d8bfc3cd3498c.

Final allocation-observer diagnosis and treatment: one-element allocation on the known no-device seat returned `cudaMalloc failed: unknown error` only under LD_PRELOAD, versus `cudaMalloc failed: no CUDA-capable device is detected` without it (observer_failure.json). Root cause: RTLD_NEXT from the preload cannot find a cudart dependency in the Python extension RTLD_LOCAL scope. Resolved symbols through the pinned toolkit handle; rebuilt ONLY observer, resealed manifest. The identical failing allocation now preserves the native no-device error (observer_fixed.json). No successful GPU allocation or computation occurred. All 20 CPU tests re-passed; final receipt CPU_GATES_FINAL.json supersedes the pre-fix build hash without rewriting old receipts. The six final mutation source hashes remain current.

## 2026-09-06 r2 — amendment 2 accepted before gates

Lead order orders/APA_SP3_AMENDMENT_2.md SHA256 20a1f49798410bd703763f3fcd3daba1b9e0eea662ec2184e4b061ca877b0407 authorizes PROTOCOL-2. Base registration and original order remain immutable. Historical 20.065/19.817 on 8 GB RTX 3070 are context only; no further recovery search. Pre-edit inventory: artifacts/apa_sp3/r2_before.json. Seat gpt-6-astra, xhigh, logs/apa_sp3_r2.log. No git or subagents.

Pre-gate implementation decisions: fixed full-prefill feeding for every arm, six disjoint 1024-token windows at offsets 0,1024,...,5120; last 512 in-input targets per window, float64 log-softmax and pooled NLL / 3072 targets. Long prefills start at token 0. C calibration and G2 keep their registered single-prefix, all-layer scope; scored six-window PPL is separate from first-prefix diagnostic fractions. New immutable protocol amendment will pin the lead order, registration, corpus Arrow file, tokenizer inputs, token file and raw little-endian bytes. G0 adds four fresh worker cells A1,B1,A2,B2 then an aggregation cell: each repeat difference <=0.001 PPL; baseline is repeat 1, without averaging away a miss. Existing in-process A/B safeguard compares to fresh baselines at the amended 0.001 tolerance. B-A +/-0.3 is only a prediction. All other rails stay fixed.

Code inspection / reference: /mnt/ForgeRealm/GraftRepository/tests/minicpm3_bulkbits_floor.py::get_text joins cached test rows by newline. Its window_nll starts at pos=512 and scores 511 targets; explicit amendment 2 requires 512, so use logits 511:1023 for targets 512:1024. Whole-prefill feeding is expressly allowed by amendment 2 and preserves the existing SP3 seam. MiniCPM3_TC.from_pretrained lines 434-439 selects BlockTC.COMPUTE_DTYPE and LinearTC.DTYPE bfloat16; no dtype change required. Cached Arrow metadata inspection: 4358 rows, 1289979 characters. Tokenizer default test adds BOS <s> id 1, no EOS; whole-stream verification will be recorded with the amendment.

Prior art: GraftRepository minicpm3_bulkbits_floor.py (local system, 2026) supplies dataset join, WINDOW/SCORED/N_WINDOWS, and teacher-forced next-token scoring reference. SP3 changes the explicit target count to 512 and uses full prefills / fp64 per lead amendment. Standard NLL/perplexity (Shannon 1948), immutable content hashes, repeated-process determinism checks and controlled comparisons are reused; no new algorithm claimed. Existing BLASST, ThriftAttention, FlashAttention-2/online normalization, TurboQuant, APA and SP2 attributions remain unchanged. This r2 author baseline is not lead-owned blind verification.

PROTOCOL-2 registration completed before CPU/GPU gates: artifacts/apa_sp3/protocol_amendment.json SHA256 db95e3ecb958ef6c31a105ee77be19c455ae8b210ed8a8082194389294843224. Canonical stream artifacts/apa_sp3/protocol2_tokens.npy: 333337 tokens, file SHA256 5684e72cbab28236391ebcac9113aeca5b318ce6eeed69c410f1094950269a28, raw token bytes SHA256 d3c64882a9cbd4264a21f60ac239680d3a7d0631ab06d0d8a5d4fac8286d268a. Defaults verified over the WHOLE stream versus add_special_tokens=False: exactly BOS <s> id 1, no EOS; no per-window additions. Creation log protocol2_registration.log records installed-transformers config/RoPE warnings; no model load attempted. AutoTokenizer uses local_files_only=True, trust_remote_code=False; these suppress network/custom-code prompts without changing special-token defaults. BF16 default remains unchanged. Full manifest digest is pinned in code in addition to registration/order/input/file/raw-byte pins, preventing forged metadata or a RECOVERED_ORIGINAL downgrade.

New tests registered before execution: test_protocol2_forged_amendment_red, test_protocol2_stale_amendment_red, test_protocol2_wrong_stream_sha_red; semantic dtype/rank/length/vocabulary guards; six-window disjoint input and pooled-NLL oracle; float64 logaddexp oracle; long-prefix target tests; fresh-process and target-count failures; B-A prediction-only acceptance. All old test assertions/tolerances retained except the historical G0 fixture now targets the amended determinism rule. Mutation seeds/80% threshold unchanged; rerun in a new create-only protocol2 lane.

R2 final CPU results (unit test / host compile / code inspection): first registered baseline 47/47 PASS (cpu_protocol2_baseline.log); added independent every-arm/window dispatch coverage before final tests. Full host CUDA compile/link PASS, command `timeout --kill-after=5s 575s env PYTHONDONTWRITEBYTECODE=1 bash scripts/apa_sp3_build.sh`, build_protocol2.log. Final rebuilt suite 57/57 PASS, 0 failed, 0 skipped (cpu_protocol2_final.log and, after report wording cleanup, cpu_protocol2_delivery.log). Final receipt CPU_GATES_PROTOCOL2.json. This is author baseline evidence, not native GPU numeric validation or blind verification.

Mutation run: `timeout 180s env PYTHONDONTWRITEBYTECODE=1 OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 python3 scripts/apa_sp3_mutations.py protocol2`; immutable mutation_manifest_protocol2.json registered before baseline/defects; all six nonerror mutants killed, rate 1.0 >=0.8, mutation_results_protocol2.json. Common/model/metrics source hashes still match the manifest. Mutations were disposable copies only, production source unchanged.

Guard rejection receipt guard_rejections_protocol2.json records verbatim: forged metadata -> `forged or stale protocol amendment SHA`; stale registration -> `protocol amendment is not bound to this registration and original source`; wrong stream file -> `protocol pin failed: tokens_path`; wrong raw bytes -> `canonical token SHA mismatch`. Deeper stream tests repin ONLY the outer digest in isolated temporary fixtures to exercise the independent file/raw-byte checks, never in production. All malformed inputs RED as required. No original registration/amendment/token/source bytes modified by these attacks.

Fresh host observation host_protocol2.json: cudaGetDeviceCount=100, device_count=0, `no CUDA-capable device is detected`. CPU imports of current engine, diagnostics and six pinned adapter modules PASS; weight stat matches the earlier full-SHA receipt. Failed one-element allocation preserved the same native no-device error with and without observer preload. No successful GPU allocation/computation, no model instantiation. `timeout 20s env PYTHONDONTWRITEBYTECODE=1 python3 scripts/apa_sp3_control.py preflight g0_A_1` now PASS before lease/device access. GPU_BLOCKED_PROTOCOL2.json supersedes the historical r1 blocked receipt without overwriting it. Fresh A/B, kernel96 and all GPU cells remain UNRUN; model quality RED / not established.

Changed script sites: apa_sp3_protocol2.py:22 create-only offline stream/manifest (new); apa_sp3_common.py:19,94,188 pinned amendment status and dependency stream validation; apa_sp3_model.py:28,47,135 exact 512-target NLL, six-window pooling, adapter BF16/g128 check; apa_sp3_gpu.py:24,112,132,215,321 fresh baseline cells / determinism / uniform scored windows / fresh-process identities; apa_sp3_control.py:19 protocol check for every non-kernel cell; apa_sp3_mutations.py:32 new create-only protocol2 output lane with unchanged seeds/threshold; apa_sp3_report.py:30 current protocol, evidence tables and commands. tensor_cuda/tests/test_apa_sp3.py:159 onward contains amendment/scoring/dispatch/determinism checks. Byte receipt changes_protocol2.json.

Unchanged: registration SHA d9b6511702a894f72174795141c72b2097b1bbe6110e810a1d4d8bfc3cd3498c; both immutable orders; 24 production files and 107 pre-existing kernel bodies; model/snapshot/INT4 g128/BF16; arm definitions, C delta grid/tie/bisection/0.01/frozen-prefix rules, E all-key real-max rule, G2 native error/masks, G3 decode, D rail, both prediction texts. S1's historical-protocol premise is explicitly historical after amendment 2, with no replacement prediction; P2 refers to fresh B. Metrics, diagnostics, observer, build script and leased shell runner are byte-identical to r1. Four fresh baseline jobs added to the existing 688 cells; all 692 retained in dependency order, primary before optional bulk8.

Delivery: `timeout 30s bash scripts/apa_sp3_lead_gpu.sh list` -> lead_list_protocol2.json; `timeout 30s bash scripts/apa_sp3_lead_gpu.sh summary` -> RESULTS.md/results.json/margins.json/dry_run.json/lead_commands.txt. delivery_protocol2.json verifies all 692 exact commands match the registry, every dependency precedes its consumer, all PPL values remain null, 0 GPU jobs, current CPU fingerprint and protocol/source pins. Lead order starts kernel96, g0_A_1, g0_B_1, g0_A_2, g0_B_2, g0, ppl_b4_D_1024; complete remaining order and individual estimates are in lead_commands.txt. Worker estimates (unmeasured): kernel 2-20s; fresh A/B 60-480s each; G0 aggregate 1-10s; model jobs with retained in-process controls 140-480s; margins 3-60s at 1024 or 30-480s at 8192. Add 30s foreground cooldown and up to 60s lease/setup/receipt overhead, enforced outer ceiling 590s.

Deviations/findings: explicit lead-authorized full prefills, all 512 targets and fp64 replace the floor scorer's cached 64-token steps / 511 targets / fp32; no dtype change. Exact cached Arrow is read directly to avoid writes/locks in the read-only HF tree, with the same row join. C/G2 diagnostic fractions retain single-prefix scope and are NOT claimed to cover six scored windows. Repeated in-process guards now cost twelve prefills; timeout/OOM remains RED, no fallback. Standard full 32768 prefill requires 80 GiB for one 40-head BF16 score tensor and cannot fit 12 GB; this is allocation reasoning about the registered cache-setup path, not an observed GPU OOM or a statement about alternate cached decode. G2/G3 establish nothing about model quality by themselves.

Prior art final: unchanged A SDPA (Vaswani et al. 2017), B APA (David Perry 2026) + TurboQuant (Zandieh et al. 2025), C/D BLASST running-max (Yuan et al. 2025/2026) with online normalization (Milakov/Gimelshein 2018; Dao 2023), E SP2's conditional margin, G2 ThriftAttention (Sharratt 2026) motivation and standard all-pair empirical statistics. Added local prior art is GraftRepository/tests/minicpm3_bulkbits_floor.py (2026), reused join and window/NLL reference; amendment changes explicitly documented above. Standard pooled NLL, content pins, independent-process controls and mutation tests are not new algorithms. R1 literature attributions are retained; no new literature claims or web checks in r2.

Process safety final: no git, no subagents, no background shell waits/jobs, no service changes, no foreign-process signals. All own foreground reads/builds/tests had explicit compute bounds <10 minutes and completed; no GPU runner lease/workload was launched. Read-only adapters/cache/production sources preserved. Execution model gpt-6-astra, reasoning effort xhigh (logs/apa_sp3_r2.log). GPU work and lead-owned blind verification pending; not claimed passed.

## 2026-09-06 r3 — lead amendment 3, registration before gates

Read HOUSE_RULES, immutable order and amendments 2/3. Baseline inventory r3_before.json: all ten existing GPU receipt fingerprints current before changes (eight PASS, two RED). Registration/source pins intact. New immutable amendment_004_capture_dtype.json binds amendment 3 and registers the mechanical float32 mask conversion, regression and unchanged gates before execution. This new amendment is itself included by the existing amendment_*.json fingerprint glob; it and the forthcoming model.py edit stale all existing job fingerprints. No receipt is rewritten or grandfathered.

Diagnosis (lead GPU receipt + code inspection): capture_b4_B_1024.json records `RuntimeError: op supports float32/float16/bfloat16 only`; diagnostic blend allocates DType::Uint8, while tc.cat supports float inputs. Plan: mask.float() on each chunk before existing tc.cat; capture already converts back to uint8. No selector, native output, packed-bit order, count rule or kernel changes. Prior art: existing published/local attributions unchanged. Routine lossless 0/1 dtype adaptation; no prior art known to me for this specific capture repair, no new algorithm or optimization claim. Test uses ordinary dependency stubs and independent expected packed-byte arithmetic; no new verification technique.

Call-site audit finding (code inspection): literal SAME call site / SAME V is NOT true in the delivered adapter. At MiniCPM3 adapter lines 283-298, B at S=1024 takes _cublas_blend_attention(q_full,k_full,kq,v64,...), intercepted by Model.blend. Model.set sets fast_max_seq=0 for C/D/E, so they take tc.apa_selective_attention(q_full,k_full,kq,vpad96,...), intercepted by Model.selective. Both branches share the same q_full/k_full/kq construction before the branch; the SP branch zero-pads the same V64 values with 32 zeros and slices the output to 64. Across separate arms later-layer activations need not be identical. Dispatch is preserved for this dtype-only amendment; a literal same-function/all-tensor identity confirmation is RED, not claimed fixed.

C target definition (code inspection): the literal instrumented B blend stores valid && abs(native_bulk)>=native_threshold, with the native threshold/statistics unchanged and native/diagnostic output byte parity checked before capture. C calibration sums selected native mask bits across all 62 layers and divides by summed eligible causal pairs on the first S=1024 prefix (all heads and queries); not nominal 0.10, not a mean of per-layer fractions, and not a fraction across six scored windows. Blend computes exact rank scores for every key; refined here means keys whose exact score is selected into the blended logits, not keys whose exact dot was computed. Actual C SP mask fraction must match within 0.01.

Process: no git, subagents, GPU workload, background jobs/waits, process signals or read-only-tree writes. Seat gpt-6-astra, xhigh (logs/apa_sp3_r3.log).

R3 reproduction and treatment receipts (unit test): cpu_capture_dtype_repro.log records both new uint8/int32 cases failing at the ORIGINAL model.py:227 with `RuntimeError: op supports float32/float16/bfloat16 only`. Changed only masks.append(mask) to masks.append(mask.float()) plus prior-art comment. cpu_capture_dtype_final.log: 59 passed, 0 failed, 0 skipped. host_capture_dtype.json verifies removing exactly the new test restores the original whole-test-file SHA, and reversing exactly the cast/comment restores the original model.py SHA: every pre-existing test and every other model byte unchanged. Native GPU capture remains RED / not claimed fixed until a card rerun.

Mutation rerun: unmodified apa_sp3_mutations.py imported with only its output ART redirected to artifacts/apa_sp3/capture_dtype_mutation_r3; unchanged final seed set/threshold 0.8, create-only manifest before fresh passing baseline, 6/6 nonerror mutants killed. No production mutation. CPU_GATES_CAPTURE_DTYPE.json supersedes the current-source applicability of CPU_GATES_PROTOCOL2.json. Prior mutation_manifest_protocol2.json model source pin is stale, retained historically. All 24 registered production files and 107 kernel bodies, adapter/module/source pins, stream/registration and weight identity verified. Existing compiled binaries/build inputs unchanged and all compiled CPU guards rerun; no unnecessary rebuild. Runtime/adapters imported on CPU. Device count API=100, count=0, `no CUDA-capable device is detected`; two failed one-element allocation probes with/without observer returned identical native error, observer_capture_dtype.json. No successful GPU allocation or model load.

Fingerprint audit (receipt_fingerprint_capture_dtype.json): all eight former PASS jobs are STALE by the unchanged exact-map rule: kernel96, g0_A_1, g0_B_1, g0_A_2, g0_B_2, g0, ppl_b4_D_1024, ppl_b4_B_8192. Differences are scripts/apa_sp3_model.py and new amendment_004_capture_dtype.json (included by existing glob). Existing RED capture_b4_B_1024 and ppl_b4_A_8192 also have stale fingerprints; original RED errors retained. require_pass rejects every former PASS with RED_OR_STALE_DEPENDENCY; all ten job-file SHA hashes still equal the pre-edit inventory. Lead owns receipt/rerun disposition; no replay, deletion, repinning or waiver performed. A/8192 remains the observed non-fit, do not retry; D/8192 depends on it and remains blocked.

R3 delivery verification: delivery_capture_dtype.json PASS_CPU_ONLY/GPU_RED. All 692 dependency-ordered command entries retained with worker estimates; GPU commands are review-only comments until the lead resolves stale/immutable receipt prerequisites. No existing PASS replay scheduled and no A/8192 retry command exists. Existing b4_B_1024 capture directory preserved. Leased runner/fingerprint/reporter scripts unchanged. Generic summary was run once (summary_capture_dtype.log), then mutable report/commands received this amendment-specific handoff; retained copies RESULTS_CAPTURE_DTYPE.md and lead_commands_capture_dtype.txt preserve it if a later generic summary regenerates its older template. Current main RESULTS.md/results.json use STALE for all ten old jobs and do not fill current PPL from historical values. Historical lead G0/D/B8192 observations are labeled explicitly. GPU_BLOCKED_CAPTURE_DTYPE.json is the fresh blocked report. Current CPU fingerprint/mutation pins and all original job hashes rechecked after artifact generation.

Final RED: GPU capture fix not card-validated; literal same-call-site/all-tensor confirmation contradicted by existing adapter dispatch; A/8192 OOM preserved and never retried; all old current dependencies stale; C/E quality, G2/G3 and lead blind verification pending. No thresholds/registration/kernel bodies altered. Prior art unchanged; mechanical cast and ordinary stub/oracle annotations are at code/test sites, ledger and final report. No git/subagents/background shell waits/jobs/service changes/process signals; no GPU model workload, successful GPU allocation, receipt deletion or replay. All started own foreground commands completed under bounds. Execution model gpt-6-astra, reasoning effort xhigh, logs/apa_sp3_r3.log.


## Amendment 5 — pool-on decode and default capture policy (2026-09-06)

Immutable plan: orders/APA_SP3_AMENDMENT_5.md. New order ledger:
`docs/APA_SP3_A5_LEDGER.md`; report: `artifacts/apa_sp3/A5_REPORT.md`.
Registered 18 decode_pool cells in amendment_009_decode_pool.json and exact
per-kind source bridge in amendment_010_pool_fingerprint.json. All prior cell
records and 148 original receipt bytes preserved; zero source-invalidated PASS.
CPU evidence: 113 passed; 5/5 registered mutants killed; 17 synthetic a4-kind
bridges accepted; 147 PASS recursively valid in original-build CPU simulation.
Current local runtime identity remains stale, not waived. Source/kernel pins
PASS. Dry-run 877 cells; default 733 excludes raw decode and all 126 32K capture
cells. Pool-state pin: test_decode_pool_worker_pool_state_pin.
GPU/P5 unrun; no 8192-based 32K estimates fabricated. Rail >=290s estimate is
terminal non-fit before lease. Pool peaks are CUDA default-pool reserved/used
high-water, not whole-device resident. Raw decode evidence remains unchanged.
Prior art: existing TensorCUDA (2026) stream-ordered transient pool; NVIDIA CUDA
12.6 pool counters (local headers verified); existing SP3 teacher-forced timing;
a4 hash bridge and Kahn scheduling; conventional cost scaling, no prior art
known to me for this exact planning formula. Historical Make/Nix/Kahn/mutation
attributions remain unverified leads detailed at code sites and A5 ledger/report.
No git, subagents, GPU leases/queries, background shell work, signals/kills,
live-service edits or live-worktree access. Bounded foreground CPU commands
completed. gpt-6-astra, xhigh; logs/apa_sp3_a5_r1.log.
