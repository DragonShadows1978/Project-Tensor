# APA-SP1 implementation ledger

2026-09-05 — implementation seat; model GPT-6 Astra (gpt-6-astra), requested reasoning effort ultra. Branch/base are dispatch metadata, not git-verified. No git, subagents, main-checkout execution, or GPU work.

Read order completed: APA mechanism and boundaries; selective fused/stats/split/merge/sink/dispatch source and named tests; immutable optimization/quant plans, synthesis and FA2 STOP; Shared HOUSE_RULES including sections 8/9. No Rust source in this worktree (rg --files -g "*.rs"). Existing test NumPy reference is the available independent pin.

Code inspection: test_selector_accuracy.py inserts the forbidden main checkout and executes GPU code at module scope; do not import unchanged here. Legacy selective tests lack no-device skips. G1 cannot honestly be fully green on this seat. Build output will stay in artifacts/apa_sp1/build, using manual compiler commands to avoid CMake network/git FetchContent and package-directory writes. Binding addition will be in src/bindings.cpp, exposed explicitly via tc._C; no second Python wrapper file needed.

Registration written before experimental code or measurements: `artifacts/apa_sp1/registration.json`, sha256 `12059d49f39abe9450b34989f06ac5ca8a736d80e3127c9d41c1ed97e650c9fe`. Source snapshots captured before edits (code inspection/hash evidence). Own predictions distinguish impossibility of irrevocable prefix decisions from unrestricted one-read buffering. CPU randomized classes are full-key row workloads, not complete attention tensors: 10,000 draws each across all registered lengths, two D and prefill/decode; causal/GQA tensor mapping separately unit-tested.

Commands so far: bounded timeout 20s cat/sed/rg/python3 source reads and registration writes; no experiment run. Memory quick pass used only for evidence-discipline context; live sources govern this order.

CPU-first baseline (unit-test evidence): `timeout 25s env PYTHONDONTWRITEBYTECODE=1 OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 python3 -m pytest -q -p no:cacheprovider tensor_cuda/tests/test_apa_sp1_reference.py` — 17 passed in 0.23s; receipt `artifacts/apa_sp1/g1_reference_initial.log`. Checks original NumPy reference at existing tolerance, literal single-visit implementation, buffered Q1 construction, suffix perturbation, ties, signed selection, order dependence, causal/GQA/VD/sink mapping and original-byte/registration pins. No CUDA equivalence inferred.

Calibration started under foreground `timeout 120s` with BLAS/OMP limited to one thread; log `artifacts/apa_sp1/calibration.log`. Only registered fraction minimization over fixed delta grid; all holdout draws remain unrun. Tool sessions may expose a running foreground command; no shell background jobs or waiting processes are launched. Build and GPU runners use the same explicit bounded foreground discipline.

Kernel addition completed (code-inspection evidence): one ascending-key warp per row, separate bulk-prefix maximum and mixed-score softmax maximum, optional diagnostic mask, denominator-only sink once, bottom-right bounds, GQA mapping, separate D/VD, fp32/fp16/bf16 specializations up to cap 512. New API is `tc._C.apa_selective_attention_sp`; only `TC_APA_SP=1` enables it. Existing launchers remain untouched. New ops.h declaration is in namespace tc before ops. `amendment_001.json` cites the registration hash and records diagnostic-memory exclusions/build protocol; no thresholds changed.

Host build started: `timeout 540s bash scripts/apa_sp1_build.sh > artifacts/apa_sp1/build.log 2>&1`; each compiler subprocess is independently bounded. No git/CMake FetchContent invoked. CPU shard first run: `timeout 540s bash scripts/apa_sp1_cpu.sh shard prefill_d64 0`; raw log `cpu_shard_first.log`.

Host-build receipt: full manual CUDA/C++ compile and link succeeded for sm_89 (`build.log`, `build/manifest.json`). CPU mutation baseline passed; 5/6 nonerror semantic mutants killed (83.33%, unit-test evidence). Exclusive-prefix mutant survives because inclusive/exclusive maxima produce identical decisions for delta>=0; this is proven equivalence, retained conservatively in denominator. No production file was mutated.

Failure receipt: initial added host-guard tests reported 15 failures + 17 passes (`g1_cpu_tests.log`), all before the SP entry: `RuntimeError: CUDA error at from_host: no CUDA-capable device is detected`. Code inspection found existing NDArray::from_host unconditionally calls cuda_check_last even for CPU tensors; no existing source changed. New host guard tests use empty CPU tensors, which take the existing zero-byte early return and still exercise every named pre-launch guard without CUDA. No numerical assertion or tolerance was weakened.

G1 existing-suite attempt (suite-run evidence): `timeout 180s env PYTHONDONTWRITEBYTECODE=1 OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 python3 scripts/apa_sp1_g1.py`, driver log `g1_driver.log`. New CPU tests: 32 passed in 0.46s. Unchanged legacy files: 49 failed, 1 passed, 48 skipped in 12.80s. Standalone selector (only unsafe import path redirected in memory): `RuntimeError: cudaMalloc failed: no CUDA-capable device is detected`. Tests were not marked skipped by this seat. Existing native no-device skips remain visible; a geometry rejection test passes on host. Full G1 is BLOCKED, not green. Exact logs and all existing test source hashes: `artifacts/apa_sp1/g1_1788656790462101850.json`.

GPU harness delivered (code inspection + CPU harness unit test): 48 registered shapes; each invocation handles one class under an exclusive `/tmp/forge-gpu.lock`, worker timeout 510s within registered 540s ceiling, outer timeout 590s + 5s signal grace, 30s foreground Python cooldown while holding lock. Busy compute processes abort without intervention. Resume only skips current-fingerprint completed receipts; failures remain explicit. Bounded dry commands `list`, `summary`, shell syntax checks passed. Harness full-reference/count check passed (1 CPU test, `harness_cpu.log`). Additional numeric thresholds unchanged. `GPU_BLOCKED.json` contains exact lead commands and the observed missing-device error.

Execution metadata correction (live log inspection, no inference): `logs/apa_sp1_astra_r1.log:5-12` reports workdir this worktree, model `gpt-6-astra`, reasoning effort **xhigh**, session `01a0742c-d1fc-79b2-80b1-0a0bef0ed56c`. The order authorized ultra; it did not change the launch configuration. Initial ledger called ultra the requested effort, not a verified runtime setting. Actual final report must say xhigh. Receipt `execution_metadata.json`; no settings edited.

CPU progress (unit-test receipts): both prefill classes and decode_d64 completed their full 10,000 draws each. decode_d128 shard 0 also passed; remaining 19 shards started as a foreground batch under timeout 540s, each child individually bounded. No overlapping random-sweep batches; single BLAS/OMP thread. Full summary waits for all 40,000 draws and validates registration/calibration/reference hashes.

Final CPU unit expansion (unit-test evidence): all 35 tests then passed (`g1_cpu_final.log`, 0.58s), including the reporter using supplied diagnostic counts rather than its own mask, a fixed-suffix wrong-first-decision output witness, and zero-query tie/sink edge. Added one explicit Q2-versus-z-score witness afterward; final rerun pending. Compiler resource extraction (`ptxas_sp1.txt`, `compiler_resources.json`): 24 new specializations compiled, all zero stack/spill bytes. Production fp32 caps D<=64 and <=128 use 46 and 54 registers respectively; this is compiler evidence only, not achieved occupancy.

Runner hardening (CPU harness evidence): malformed shape/path rejected before lease/GPU work (`runner_input_guard.json`, exit 64); lock opened append-only so lease metadata is not truncated. Shell syntax and read-only list/summary passed. GPU boundary gate explicitly covers D=512 MQA (H=16, KVH=1), sinks, ragged D/VD, all three floating dtypes, and old sink output bit-identity under the SP flag toggle. These GPU checks are delivered, unrun.

Final randomized CPU receipt (unit-test evidence): all 80 shards completed, 40,000 independent full-key row draws total, 10,000 each for prefill_d64, prefill_d128, decode_d64, decode_d128. `timeout 60s bash scripts/apa_sp1_cpu.sh summarize` validated every registration/calibration/reference hash and draw count; `cpu_summary.json` / `cpu_summary.log`. Exact mask disagreements: 0; maximum absolute online-versus-offline output difference 3.5762786865234375e-07 at frozen atol=rtol=1e-3. All 24 row geometries match refine fractions within the registered 0.02 absolute tolerance. Prefill z-tail recall 45.193998629941734%; decode 45.703050667583506%. Lead P2 overlap and decode-lower predictions MISS for the registered bulk-prefix rule; its monotone property HIT. Seat A1/A2/A3/A6 HIT. Lead P1 HIT under restricted irrevocable streaming and MISS as an unrestricted impossibility claim.

CPU numerical analogue of the registered P3 deviation threshold (unit-test evidence, NOT the G3 GPU gate): all 24 CPU row geometries have error-norm ratio <=2 versus the two-pass reference at matched fractions; range 0.4554590095712575 to 0.9442044984697119. This uses width-4 random values and pooled row draws, not the registered full GPU tensors/outputs. P3 GPU deviation and all speed predictions remain BLOCKED, not scored hits. **this establishes nothing about model quality**.

Final artifacts generated: `timeout 30s env PYTHONDONTWRITEBYTECODE=1 OPENBLAS_NUM_THREADS=1 python3 scripts/apa_sp1_report.py` wrote `CPU_REPORT.md`, `source_additions.patch` (difflib, no git) and `G1_FINAL.json`. The report names the buffered-Q1/no-splitK/no-Rust/mixed-vs-bulk-max limitations, CPU class scope, all negative predictions, exact lead commands, and actual xhigh launch effort. G2/G3 blocked receipt: `GPU_BLOCKED.json`. Independent blind verification remains lead-owned and unrun.

Last harness correction (code inspection + CPU unit test): non-finite output metrics now serialize as null fields with explicit nonfinite counts; GPU receipt publication is atomic and create-only so an interrupted worker cannot leave a partial final JSON that breaks resume. This changes reporting only, not kernel/reference/calibration semantics. `timeout 30s bash scripts/apa_sp1_cpu.sh unit` then passed all 37 tests in 1.01s (`g1_cpu_final_r3.log`). Updated shell syntax and pending GPU-summary checks passed. No runtime GPU checks were run.

Final audit: registration/calibration sha256 unchanged; all original bytes in the three existing edited files hash-pin unchanged after removing marked additions; linked module and every build-source hash match `build/manifest.json`; all new Python sources parse and shell scripts pass bash -n. No files outside the authorized edit boundary were intentionally written. All started compiler/test/sweep commands completed; no background shell jobs, git commands, subagents, GPU workloads, main-checkout/other-worktree changes, live-service changes, or foreign process termination. Remaining open work is explicitly the lead GPU gates and independent verification.

2026-09-06 — SP1.1 registration BEFORE implementation/measurements: `registration_sp1_1.json`, SHA256 `382194c1f9d94c146bddcf533b7becadee60fe7d212a2029fabf133243c683b4`; parent `12059d49f39abe9450b34989f06ac5ca8a736d80e3127c9d41c1ed97e650c9fe`. Live launch log `logs/apa_sp1_1_astra_r2.log` reports gpt-6-astra, xhigh. Source snapshots and 51 supplied GPU JSON receipts pinned. Partition width/128-thread layout inherited from existing launcher; frozen deltas retained. Mathematical clarification: partition selection is a superset, but extra exact scores can change the output; only partition-selected materialization equals merged output. This is an explicit deviation from any global-output equality interpretation. No git/subagents/GPU/foreign process termination.

SP1.1 CPU diagnosis: `sp1_1_diagnosis.1788660692574177856.json` reconstructs five mask differences, including two output-affecting keys: (h=1,q=1271,k=1199), (h=3,q=1137,k=737). Largest reconstructed error 0.0015865452587604523 matches original GPU max_abs 0.0015865489840507507 within 3.8e-9. Both dots and fp32 stats/FMA order contribute; causal/GQA ruled out for this class. Mathematical emulator unchanged; separate ordered reference and diagnostic GPU hook added. Sensitivity verdict, original G2 FAIL retained.

SP1.1 initial build passed; 52 CPU tests passed in 3.36s, including all 37 SP1 tests, 1,000 randomized partition/merge trials, counterexample to global output equivalence, specific-key diagnosis, immutable receipts and all existing kernel-body pins. Supplied lead legacy receipt reports 95 passed, 3 skipped. No new GPU execution. Initial compiler report found D64 accumulator in a 256-byte local stack despite zero reported spill stores/loads; static unrolling in the new kernel now removes dynamic accumulator indexing. Rebuild/verification required, no measured speed claim.

SP1 scoring finalized from all pinned original receipts: 47/48 G2 PASS, 34/48 fractions matched, 33/48 jointly eligible. All 14 unmatched decode classes explained in `sp1_matching_explanations.md/json`. No unmatched row supplies a matched-comparison hit. P2 overlap/lower MISS; P3 half-shape deviation HIT (33/48); P3 all-prefill speed MISS (5 counterexamples); A3 HIT; A5 MISS (5/24 below1.4). P3 decode-speed direction and A4 classified coverage MISS with observed eligible medians explicitly reported. These are not measured counterexamples. Kernel sweep: this establishes nothing about model quality.

SP1.1 final host verification: rebuild `sp1_1_build_final.log` succeeded; all 24 split-K specializations have zero stack frame/spill bytes. FP32 D64 uses 95 registers, D128 uses 40. `timeout 300s bash scripts/apa_sp1_1_cpu.sh unit` passed 52 tests in 3.44s; exact create-only receipt `sp1_1_g1_1788661437075136419.json`. Final module/source hashes match `build/manifest.json` (SHA e8530fab17294bab69b95963f98917c820d03d7bfbbccbfb013afcb9f4bc5881). Original kernel bodies and all 51 original GPU JSON receipts remain byte/hash pinned. No emulator tolerances changed.

Final deliverables: `docs/APA_SP1_1_REPORT.md`, `G1_SP1_1_FINAL.json`, `GPU_BLOCKED_SP1_1.json`, `sp1_1_lead_commands.txt` (one bounded foreground invocation per class), `gpu_summary_sp1_final.json`, `sp1_matching_explanations.md/json`, additive source patch and model/effort metadata. New G2/G3 remains BLOCKED_NO_GPU. Finalized original predictions contain no PENDING. Every one of 24 decode classes reconstructs the exact original SP and z selection counts on CPU. Summary, next-splitk and malformed-target guards were CPU-verified without lease/GPU entry. All added Python parses, shell syntax passes and report links resolve.

Process safety: no git, subagents, shell background jobs, GPU workloads, live-service changes, or foreign-process termination. All foreground compiler/test/reconstruction sessions have completed, each with explicit sub-10-minute timeout. All mutations are inside this worktree. Initial read-only AGENTS filename discovery extended to parent directories; no other-checkout source, runtime or build was used or edited. Memory lookup was read-only and supplied evidence-discipline context only. A first SASS extraction guessed one wrong mangled signature and reported no function; the subsequent source-log-derived exact symbol succeeded. No result was inferred from that empty first extraction.

RED residuals: actual GPU race/equivalence, low-precision/sink behavior and speed are lead-owned and unrun; partition resets can cause larger fractions at unchanged delta, and more refinements do not guarantee better dense deviation. CPU containment/merge identity does not imply global-prefix output identity. Actual launch model gpt-6-astra, effort xhigh. Evidence class: kernel sweep; this establishes nothing about model quality.
